// Intentionally empty.
//
// This file used to export a paraglide `reroute` that de-localized `/en` back to
// `/`. The two locales are now plain prerendered routes (`/` = fr, `/en` = en),
// so rerouting would break the English page. Safe to delete.
export {};
